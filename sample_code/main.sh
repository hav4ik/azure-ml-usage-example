# Let's print some info
echo "hello world";
nvidia-smi;
pip freeze;
which python;

# You can install packages here (it's better to create a curated environment,
# but this way is easier for ad-hoc testing and debugging)
pip install transformers;

# execute train.py with first argument
python train.py;
